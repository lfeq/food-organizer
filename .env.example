# Injected by the Vercel × Neon integration. Do not set these manually in
# Vercel — the integration writes them for you after you add the database.
DATABASE_URL=postgres://...
DATABASE_URL_UNPOOLED=postgres://...
